# Expedition economy implementation plan

1. Replace the 2023 raw fallbacks with a validated compact current-league snapshot; verify deterministic export and manifest hashes.
2. Build a localized base-to-unique catalog sorted by maximum chaos value; verify Mageblood/Heavy Belt and RU labels.
3. Add value thresholds and bounded automatic fillers; verify the generated expression stays one copyable regex of at most 250 characters.
4. Render dense value cards and persisted controls; verify RU/EN browser snapshots and compact price formatting.
5. Add a daily scheduled refresh that cannot affect ordinary push reliability; verify the release workflow still gates deployment on the full suite.
